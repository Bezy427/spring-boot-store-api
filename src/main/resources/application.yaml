spring:
  application:
    name: store
  jwt:
    secret: ${JWT_SECRET}
    accessTokenExpiration: 7200000 # 5 min
    refreshTokenExpiration: 604800 # 7 days
  profile:
    active: dev
stripe:
  secretKey: ${STRIPE_KEY}
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY}

